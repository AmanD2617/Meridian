{
  "name": "meridian",
  "version": "1.0.0",
  "description": "Meridian — Multi-Agent Predictive Logistics Platform (Google Solution Challenge)",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"blue,green\" --kill-others-on-fail \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "seed": "npm run seed --prefix server",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
